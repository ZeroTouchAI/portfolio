Drop your Phase 2 output here as `hero.mp4`.

Expected: an MP4 of your talking-head cinematic video, ideally under
15–20MB so it loads quickly. The hero component looks for it at
/videos/hero.mp4 (i.e. this file, named exactly `hero.mp4`).

Until you add it, the hero falls back to a warm gradient background
so the layout still looks intentional.
